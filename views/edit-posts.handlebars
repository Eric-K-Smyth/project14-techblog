<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Blog Post</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <header>
    <h1>Edit Blog Post</h1>
  </header>
  <main>
    {{#if logged_in}}
    <section class="edit-post">
      <form id="edit-post-form" action="/dashboard/update/{{post.id}}" method="POST"> <!-- Specify the update route with the post ID -->
        <input type="text" id="post-title" name="title" placeholder="Post Title" value="{{post.title}}">
        <textarea id="post-body" name="body" placeholder="Post Content">{{post.body}}</textarea>
        <button type="submit">Save Changes</button>
      </form>
    </section>
    {{else}}
    <p>Please log in to edit blog posts.</p>
    {{/if}}
  </main>
  <footer>
    <!-- Footer content here -->
  </footer>
</body>
</html>

