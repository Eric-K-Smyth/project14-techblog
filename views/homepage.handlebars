<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog - Homepage</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
        <h1>Welcome to the Tech Blog</h1>
    </header>
    <main>
        {{#if logged_in}}
        <h2>Recent Posts</h2>
        <ul>
            {{#each posts}}
            <li>
                <a href="/post/{{id}}">{{title}}</a> by {{User.username}}
            </li>
            {{/each}}
        </ul>
        {{else}}
        <p>Please <a href="/login">Login</a> or <a href="/login">Signup</a> to see blog posts.</p>
        {{/if}}
    </main>
    <footer>
        <!-- Footer content here -->
    </footer>
</body>
</html>
